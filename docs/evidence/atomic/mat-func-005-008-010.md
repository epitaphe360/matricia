# Preuves atomiques — MAT-FUNC-005, 008 et 010

Date : 2026-09-13. Cible SQL : Supabase **development** liée. Aucun accès production.

## MAT-FUNC-005 — diagnostic continu

- Contrat : `question_versions.validity_interval` fixe la durée de validité ; chaque révision immuable porte `answered_at` et `expires_at`.
- Autorité serveur : la version finale de `submit_questionnaire_session` (migration `127`) compte une réponse requise expirée comme manquante.
- Produit : le repository expose `requiresRevalidation`, exclut les réponses expirées de la progression et l’UI FR/AR affiche une alerte, bloque la soumission puis permet de confirmer la valeur existante. L’autosave crée une nouvelle révision et renouvelle l’expiration selon la version de question.
- Preuves : tests repository/UI couvrant expiration, progression et rendu arabe ; ancien moteur SQL `0025`/`0054` et durcissement final `127`.
- Statut candidat : fonctionnel et audité sans P0/P1/P2. Un E2E post-migration dédié reste requis avant promotion `VERIFIED`.

## MAT-FUNC-008 — assistant création besoin

- Migration : `20260913016000_assistance_discovery_and_bundle_projection.sql`.
- Contrat : `discover_assistance_scope` transforme un texte FR/AR borné en au plus 20 versions de service publiées, puis au plus 50 versions de question liées à ces services.
- Données manquantes : les `known_data_keys` et les réponses non nulles encore valides de l’organisation sont exclues ; une réponse expirée redevient candidate.
- Sécurité : rôle Client d’analyse exigé, organisation étrangère refusée, entrée et limites validées, `SECURITY DEFINER` à `search_path` fixe, ACL `authenticated` seulement. L’index partiel `question_versions_published_service_discovery_idx` borne la recherche par services sélectionnés ; jamais de chargement des 6 000 questions.
- Décision humaine : la découverte ne crée aucun besoin, RFQ ou mission. Les candidats alimentent `run_assisted_analysis`, déjà idempotent, audité et outboxé ; chaque suggestion exige une acceptation ou un rejet et conserve `business_action_executed=false`.
- Preuves : `0105_assistance_discovery_bundle_projection.test.sql` et tests repository/Server Actions/UI.
- Statut candidat : fonctionnel et audité sans P0/P1/P2. E2E authentifié post-migration à relancer avant promotion `VERIFIED`.

## MAT-FUNC-010 — bundles multi-bibliothèques

- Contrat historique : `solution_bundle_versions` et `solution_bundle_items` sont versionnés, immuables, dédupliqués et conservent `source_manifest_hash`, politique de fusion, bibliothèques et provenance.
- Projection : `list_solution_bundles_v1` est `SECURITY INVOKER`; les RLS existantes filtrent le tenant et les montants `bigint` sont projetés en `TEXT` exact.
- Défense applicative : le repository recoupe chaque `organization_id` avec les memberships courants et échoue fermé sur une réponse RPC étrangère ou mal formée.
- Produit : l’écran Solutions FR/AR affiche titre/version, bibliothèques, total exact, manifeste, stratégie de fusion et provenance. Il rappelle qu’aucune commande n’est créée sans décision humaine explicite.
- Preuves : SQL `0075`, `0085`, `0105`; tests repository (incluant réponse étrangère) ; build Next complet.
- Statut candidat : fonctionnel et audité sans P0/P1/P2. E2E authentifié post-migration à relancer avant promotion `VERIFIED`.

## Gates exécutés

- Web ciblé : 9 fichiers, 29 tests, PASS.
- TypeScript strict : PASS.
- ESLint Web : PASS.
- Build Next : PASS.
- PostgreSQL ciblé `0105` + `0106` : 2 fichiers, 42 assertions, PASS (`0105` contient 13 assertions A1).
- Migration `160` appliquée sur development avec `161` déjà auditée ; dry-run final : `upToDate=true`.
- `git diff --check` : PASS.
- Audit indépendant : faux positif sur l’expiration retiré après vérification de la définition finale `127`; produit A1 sans P0/P1/P2.

## Risque résiduel et prochaine action

Le runner E2E P24 existant n’a pas pu être relancé après migration car son processus Windows échoue avant le démarrage avec `helper_unknown_error: setup refresh had errors`. Relancer P24 FR/AR, 360 px et desktop dès rétablissement du lanceur, puis promouvoir les trois IDs uniquement si le parcours authentifié et l’isolation tenant sont verts.
