import{getSupabaseServerClient}from"@/lib/supabase/server";import{createSolutionInsightsRepository}from"./repository";
export async function createServerSolutionInsightsRepository(){const c=await getSupabaseServerClient();return createSolutionInsightsRepository({async user(){const{data}=await c.auth.getUser();return data.user?.id??null},async sets(){return await c.from("diagnostic_solution_sets").select("id,anomaly_id,version,rationale_fr,rationale_ar,created_at").order("created_at",{ascending:false}).limit(100)},async options(ids){return ids.length?await c.from("diagnostic_solution_options").select("id,solution_set_id,level,expected_score_bps,estimated_amount_minor,currency,benefits,tradeoffs,explanation").in("solution_set_id",ids).limit(300):{data:[],error:null}},async decisions(ids){return ids.length?await c.from("diagnostic_solution_decisions").select("id,solution_set_id,level,decision,reason,deferred_until,decided_at").in("solution_set_id",ids).order("decided_at",{ascending:false}).limit(300):{data:[],error:null}},async benchmarks(){return await c.from("benchmark_publications").select("id,metric_code,segment_key,period_start,period_end,group_size_band,rounded_mean,published_at").order("published_at",{ascending:false}).limit(100)},async rpc(name,input){return await c.rpc(name,input)}})}
