import{z}from"zod";export const uuid=z.string().uuid(),hash=z.string().regex(/^[0-9a-f]{64}$/),money=z.coerce.number().int().min(-Number.MAX_SAFE_INTEGER).max(Number.MAX_SAFE_INTEGER);export const submitContractSchema=z.object({contractId:uuid,rowVersion:z.coerce.number().int().positive(),idempotencyKey:uuid});export const signContractSchema=z.object({contractId:uuid,contractVersionId:uuid,organizationId:uuid,contractHash:hash,signerRole:z.string().trim().min(2).max(80),method:z.string().trim().min(2).max(80),signedAt:z.string().datetime(),evidenceHash:hash,idempotencyKey:uuid});export const missionSchema=z.object({contractId:uuid,contractVersionId:uuid,milestoneKey:z.string().trim().regex(/^[A-Z][A-Z0-9_-]{1,79}$/),titleFr:z.string().trim().min(2).max(300),titleAr:z.string().trim().min(2).max(300),dueAt:z.union([z.literal(""),z.string().datetime()]),deliverableKey:z.string().trim().regex(/^[A-Z][A-Z0-9_-]{1,79}$/),labelFr:z.string().trim().min(2).max(300),labelAr:z.string().trim().min(2).max(300),criterionKey:z.string().trim().regex(/^[A-Z][A-Z0-9_-]{1,79}$/),idempotencyKey:uuid});export const acceptanceSchema=z.object({deliverableId:uuid,criteria:z.array(z.object({criterion_key:z.string().min(2),status:z.enum(["COMPLIANT","NONCOMPLIANT","NA"]),comment:z.string().max(1000),evidence:z.array(z.unknown())})).min(1),idempotencyKey:uuid});export const amendmentSchema=z.object({contractId:uuid,reason:z.string().trim().min(3).max(2000),scopeDelta:z.string().trim().min(2).max(4000),priceDeltaMinor:money,deadlineDeltaDays:z.coerce.number().int().min(-3650).max(3650),idempotencyKey:uuid});
export type ContractMissionDashboard={organizationId:string;organizationName:string;contracts:Array<{id:string;providerOrganizationId:string;status:string;currentVersion:number;rowVersion:number;versionId:string;priceMinor:string;currency:string;contentHash:string;signatureCount:number}>;missions:Array<{id:string;contractId:string;status:string;startedAt:string|null;completedAt:string|null;milestones:Array<{id:string;key:string;title:string;status:string;dueAt:string|null}>;deliverables:Array<{id:string;key:string;label:string;status:string;currentVersion:number;criteria:Array<{key:string;status:string}>}>}>};export function formatExactMoney(minor:string,currency:string,locale:"fr"|"ar"){const n=BigInt(minor),hundred=BigInt(100),whole=n/hundred,fraction=(n<0?-(n%hundred):n%hundred).toString().padStart(2,"0"),tag=locale==="ar"?"ar-MA":"fr-MA";const integer=new Intl.NumberFormat(tag,{maximumFractionDigits:0}).format(whole);return new Intl.NumberFormat(tag,{style:"currency",currency,minimumFractionDigits:2}).formatToParts(0).map(p=>p.type==="integer"?integer:p.type==="fraction"?fraction:p.value).join("")}
