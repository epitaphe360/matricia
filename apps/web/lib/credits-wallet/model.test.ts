import{describe,expect,it}from"vitest";import{hash,positiveInteger}from"./model";describe("credit value objects",()=>{it("accepts unbounded integer strings without number conversion",()=>{expect(positiveInteger.parse("900719925474099300000")).toBe("900719925474099300000");expect(positiveInteger.safeParse("1.5").success).toBe(false)});it("requires immutable proof hashes",()=>{expect(hash.safeParse("a".repeat(64)).success).toBe(true);expect(hash.safeParse("secret").success).toBe(false)})});
