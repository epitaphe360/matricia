import{z}from"zod";import{getSupabaseServerClient}from"@/lib/supabase/server";import{categoryRows,centerPayload,type NotificationDashboard}from"./model";const member=z.object({organization_id:z.string().uuid(),organizations:z.object({display_name:z.string().min(1)})});export async function loadNotificationCenter(locale:"fr"|"ar"):Promise<{status:"success";dashboard:NotificationDashboard}|{status:"error";reason:"UNAUTHENTICATED"|"NO_ORGANIZATION"|"QUERY_FAILED"|"INVALID_RESPONSE"}>{const c=await getSupabaseServerClient();const{data:a}=await c.auth.getUser();if(!a.user)return{status:"error",reason:"UNAUTHENTICATED"};const mr=await c.from("organization_memberships").select("organization_id,organizations!inner(display_name)").eq("user_id",a.user.id).eq("status","ACTIVE").limit(1).maybeSingle();if(mr.error)return{status:"error",reason:"QUERY_FAILED"};const m=member.safeParse(mr.data);if(!m.success)return{status:"error",reason:mr.data?"INVALID_RESPONSE":"NO_ORGANIZATION"};const[center,categories]=await Promise.all([c.rpc("list_notification_center",{p_organization_id:m.data.organization_id,p_limit:100}),c.from("notification_preference_categories").select("code,label_fr,label_ar,mandatory").eq("status","ACTIVE").order("code")]);if(center.error||categories.error)return{status:"error",reason:"QUERY_FAILED"};const cp=centerPayload.safeParse(center.data),cr=categoryRows.safeParse(categories.data);if(!cp.success||!cr.success)return{status:"error",reason:"INVALID_RESPONSE"};return{status:"success",dashboard:{organizationId:m.data.organization_id,organizationName:m.data.organizations.display_name,categories:cr.data.map(x=>({code:x.code,label:locale==="ar"?x.label_ar:x.label_fr,mandatory:x.mandatory})),preferences:cp.data.preferences,notifications:cp.data.notifications}}}
