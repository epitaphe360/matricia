import { z } from "zod";
import { getSupabaseServerClient } from "@/modules/shared/lib/supabase/server";
import { sumExact, type FranchiseDashboard } from "./model";

const id = z.string().uuid(), minor = z.union([z.string().regex(/^-?\d+$/), z.number().int().safe()]), ledgerId = z.union([z.string().regex(/^\d+$/), z.number().int().safe()]);
const franchise = z.object({ id, library_id: id, operator_organization_id: id, franchise_type: z.enum(["IT", "STANDARD"]), operator_code: z.string(), territory_code: z.string(), status: z.string(), current_mandate_version_id: id.nullable(), row_version: z.number().int().positive() });
const library = z.object({ id, code: z.string() });
const territory = z.object({ id, franchise_id: id, version: z.number().int().positive(), territory_code: z.string(), name_fr: z.string(), name_ar: z.string(), effective_from: z.string(), effective_until: z.string().nullable(), content_hash: z.string().regex(/^[0-9a-f]{64}$/) });
const mandate = z.object({ id, franchise_id: id, territory_version_id: id, economic_rule_version_id: id, version: z.number().int().positive(), mandate_status: z.string(), entry_fee_minor: minor, entry_fee_mode: z.string(), franchisee_share_bps: z.number().int(), neoxa_share_bps: z.number().int(), matricia_share_bps: z.number().int(), effective_from: z.string(), effective_until: z.string().nullable(), content_hash: z.string().regex(/^[0-9a-f]{64}$/) });
const approval = z.object({ id, franchise_id: id, mandate_version_id: id, request_type: z.string(), status: z.string(), change_reason: z.string(), requested_at: z.string(), decided_at: z.string().nullable(), row_version: z.number().int().positive() });
const approvalDecision = z.object({ approval_request_id: id, decision: z.enum(["APPROVE", "REJECT"]), reason: z.string(), mandate_content_hash: z.string().regex(/^[0-9a-f]{64}$/), decided_at: z.string() });
const invitation = z.object({ id, franchise_id: id, invited_email: z.string(), status: z.string(), expires_at: z.string(), row_version: z.number().int().positive() });
const rule = z.object({ id, franchise_type: z.enum(["IT", "STANDARD"]), version: z.number().int(), entry_fee_required: z.boolean(), franchisee_share_bps: z.number().int(), neoxa_share_bps: z.number().int(), asma_matricia_share_bps: z.number().int(), effective_from: z.string() });
const book = z.object({ id, library_id: id, franchise_organization_id: id, franchise_type: z.enum(["IT", "STANDARD"]), operator_code: z.string(), currency: z.string(), opened_under_rule_version_id: id, status: z.string() });
const fee = z.object({ id, book_id: id, mode: z.string(), principal_minor: minor, deposit_minor: minor, starts_on: z.string(), due_on: z.string() });
const feeLedger = z.object({ id: ledgerId, schedule_id: id, entry_type: z.string(), amount_minor: minor, created_at: z.string() });
const closure = z.object({ id, book_id: id, period_start: z.string(), period_end: z.string(), version: z.number().int(), supersedes_closure_id: id.nullable(), rule_version_id: id, entry_cutoff_at: z.string(), gross_revenue_ex_tax_minor: minor, costs_and_refunds_minor: minor, distributable_profit_minor: minor, content_hash: z.string().regex(/^[0-9a-f]{64}$/), closed_at: z.string() });
const allocation = z.object({ id, closure_version_id: id, beneficiary_code: z.string(), share_bps: z.number().int(), amount_minor: minor, rounding_adjustment_minor: z.number().int() });
const ledger = z.object({ id: ledgerId, book_id: id, closure_version_id: id.nullable(), beneficiary_code: z.string(), entry_type: z.string(), amount_minor: minor, reference_type: z.string(), reference_id: z.string(), proof_hash: z.string().regex(/^[0-9a-f]{64}$/).nullable(), created_at: z.string() });

export type FranchiseLoadResult = { status: "success"; dashboard: FranchiseDashboard } | { status: "error"; reason: "UNAUTHENTICATED" | "FORBIDDEN" | "QUERY_FAILED" | "INVALID_RESPONSE" };

export async function loadFranchiseDashboard(locale: "fr" | "ar"): Promise<FranchiseLoadResult> {
  const client = await getSupabaseServerClient(), { data: auth } = await client.auth.getUser(); if (!auth.user) return { status: "error", reason: "UNAUTHENTICATED" };
  const platform = await client.from("platform_user_roles").select("role_code").eq("user_id", auth.user.id).is("revoked_at", null).limit(20); if (platform.error) return { status: "error", reason: "QUERY_FAILED" };
  const roles = new Set((platform.data ?? []).map((value: { role_code: string }) => value.role_code));
  const [fs, ls, ts, ms, aps, decisionsResult, ins, rs, bs, fees, cs, als, ledgers, feeLedgersResult] = await Promise.all([
    client.from("franchises").select("id,library_id,operator_organization_id,franchise_type,operator_code,territory_code,status,current_mandate_version_id,row_version").order("created_at", { ascending: false }).limit(100),
    client.from("catalog_libraries").select("id,code").limit(100),
    client.from("franchise_territory_versions").select("id,franchise_id,version,territory_code,name_fr,name_ar,effective_from,effective_until,content_hash").order("version", { ascending: false }).limit(300),
    client.from("franchise_mandate_versions").select("id,franchise_id,territory_version_id,economic_rule_version_id,version,mandate_status,entry_fee_minor,entry_fee_mode,franchisee_share_bps,neoxa_share_bps,matricia_share_bps,effective_from,effective_until,content_hash").order("version", { ascending: false }).limit(300),
    client.from("franchise_approval_requests").select("id,franchise_id,mandate_version_id,request_type,status,change_reason,requested_at,decided_at,row_version").order("requested_at", { ascending: false }).limit(200),
    client.from("franchise_approval_decisions").select("approval_request_id,decision,reason,mandate_content_hash,decided_at").order("decided_at", { ascending: false }).limit(200),
    client.from("franchise_invitations").select("id,franchise_id,invited_email,status,expires_at,row_version").order("created_at", { ascending: false }).limit(200),
    client.from("franchise_finance_rule_versions").select("id,franchise_type,version,entry_fee_required,franchisee_share_bps,neoxa_share_bps,asma_matricia_share_bps,effective_from").eq("status", "ACTIVE").order("version", { ascending: false }).limit(50),
    client.from("franchise_pnl_books").select("id,library_id,franchise_organization_id,franchise_type,operator_code,currency,opened_under_rule_version_id,status").limit(100),
    client.from("franchise_entry_fee_schedules").select("id,book_id,mode,principal_minor,deposit_minor,starts_on,due_on").limit(100),
    client.from("franchise_profit_closure_versions").select("id,book_id,period_start,period_end,version,supersedes_closure_id,rule_version_id,entry_cutoff_at,gross_revenue_ex_tax_minor,costs_and_refunds_minor,distributable_profit_minor,content_hash,closed_at").order("closed_at", { ascending: false }).limit(300),
    client.from("franchise_profit_allocations").select("id,closure_version_id,beneficiary_code,share_bps,amount_minor,rounding_adjustment_minor").limit(900),
    client.from("franchise_distribution_ledger_entries").select("id,book_id,closure_version_id,beneficiary_code,entry_type,amount_minor,reference_type,reference_id,proof_hash,created_at").order("id", { ascending: false }).limit(2000),
    client.from("franchise_entry_fee_ledger_entries").select("id,schedule_id,entry_type,amount_minor,created_at").order("id", { ascending: false }).limit(500),
  ]);
  if ([fs, ls, ts, ms, aps, decisionsResult, ins, rs, bs, fees, cs, als, ledgers, feeLedgersResult].some((result) => result.error)) return { status: "error", reason: "QUERY_FAILED" };
  const parsed = [z.array(franchise).safeParse(fs.data), z.array(library).safeParse(ls.data), z.array(territory).safeParse(ts.data), z.array(mandate).safeParse(ms.data), z.array(approval).safeParse(aps.data), z.array(approvalDecision).safeParse(decisionsResult.data), z.array(invitation).safeParse(ins.data), z.array(rule).safeParse(rs.data), z.array(book).safeParse(bs.data), z.array(fee).safeParse(fees.data), z.array(closure).safeParse(cs.data), z.array(allocation).safeParse(als.data), z.array(ledger).safeParse(ledgers.data), z.array(feeLedger).safeParse(feeLedgersResult.data)] as const;
  if (parsed.some((result) => !result.success)) return { status: "error", reason: "INVALID_RESPONSE" };
  const [franchises, libraries, territories, mandates, approvals, decisions, invitations, rules, books, schedules, closures, allocations, entries, feeLedgers] = parsed.map((result) => result.success ? result.data : []) as [z.infer<typeof franchise>[], z.infer<typeof library>[], z.infer<typeof territory>[], z.infer<typeof mandate>[], z.infer<typeof approval>[], z.infer<typeof approvalDecision>[], z.infer<typeof invitation>[], z.infer<typeof rule>[], z.infer<typeof book>[], z.infer<typeof fee>[], z.infer<typeof closure>[], z.infer<typeof allocation>[], z.infer<typeof ledger>[], z.infer<typeof feeLedger>[]];
  if (!franchises.length && !roles.size) return { status: "error", reason: "FORBIDDEN" };
  return { status: "success", dashboard: {
    canApprove: roles.has("SUPER_ADMIN") || roles.has("MATRICIA_ADMIN"), canManageFinance: roles.has("SUPER_ADMIN") || roles.has("FINANCE_MANAGER"),
    franchises: franchises.map((f) => {
      const lib = libraries.find((l) => l.id === f.library_id), m = mandates.find((value) => value.id === f.current_mandate_version_id) ?? mandates.find((value) => value.franchise_id === f.id);
      return { id: f.id, libraryCode: lib?.code ?? "—", libraryNameFr: lib?.code ?? "—", libraryNameAr: lib?.code ?? "—", organizationId: f.operator_organization_id, type: f.franchise_type, operatorCode: f.operator_code, territoryCode: f.territory_code, status: f.status, rowVersion: f.row_version, mandate: m ? { id: m.id, version: m.version, status: m.mandate_status, entryFeeMinor: String(m.entry_fee_minor), entryFeeMode: m.entry_fee_mode, franchiseeShareBps: m.franchisee_share_bps, neoxaShareBps: m.neoxa_share_bps, asmaShareBps: m.matricia_share_bps, effectiveFrom: m.effective_from } : null, territoryVersions: territories.filter((value) => value.franchise_id === f.id).map((value) => ({ id: value.id, version: value.version, code: value.territory_code, name: locale === "ar" ? value.name_ar : value.name_fr, effectiveFrom: value.effective_from, effectiveUntil: value.effective_until, contentHash: value.content_hash })), mandateVersions: mandates.filter((value) => value.franchise_id === f.id).map((value) => ({ id: value.id, territoryVersionId: value.territory_version_id, economicRuleVersionId: value.economic_rule_version_id, version: value.version, status: value.mandate_status, entryFeeMinor: String(value.entry_fee_minor), entryFeeMode: value.entry_fee_mode, franchiseeShareBps: value.franchisee_share_bps, neoxaShareBps: value.neoxa_share_bps, asmaShareBps: value.matricia_share_bps, effectiveFrom: value.effective_from, effectiveUntil: value.effective_until, contentHash: value.content_hash })) };
    }),
    approvals: approvals.map((v) => { const decision = decisions.find((value) => value.approval_request_id === v.id); return { id: v.id, franchiseId: v.franchise_id, mandateVersionId: v.mandate_version_id, requestType: v.request_type, status: v.status, reason: v.change_reason, requestedAt: v.requested_at, decidedAt: v.decided_at, rowVersion: v.row_version, decision: decision ? { decision: decision.decision, reason: decision.reason, mandateContentHash: decision.mandate_content_hash, decidedAt: decision.decided_at } : null }; }), invitations: invitations.map((v) => ({ id: v.id, franchiseId: v.franchise_id, email: v.invited_email, status: v.status, expiresAt: v.expires_at, rowVersion: v.row_version })), rules: rules.map((v) => ({ id: v.id, franchiseType: v.franchise_type, version: v.version, entryFeeRequired: v.entry_fee_required, franchiseeShareBps: v.franchisee_share_bps, neoxaShareBps: v.neoxa_share_bps, asmaShareBps: v.asma_matricia_share_bps, effectiveFrom: v.effective_from })),
    books: books.map((b) => {
      const bookClosures = closures.filter((v) => v.book_id === b.id), latest = bookClosures[0], closureAllocations = latest ? allocations.filter((v) => v.closure_version_id === latest.id) : [], schedule = schedules.find((v) => v.book_id === b.id), bookEntries = entries.filter((v) => v.book_id === b.id), beneficiaryCodes = [...new Set(bookEntries.map((v) => v.beneficiary_code))], feeMovements = schedule ? feeLedgers.filter((v) => v.schedule_id === schedule.id) : [];
      return { id: b.id, libraryId: b.library_id, organizationId: b.franchise_organization_id, type: b.franchise_type, operatorCode: b.operator_code, currency: b.currency, ruleVersionId: b.opened_under_rule_version_id, status: b.status, entryFee: schedule ? { mode: schedule.mode, principalMinor: String(schedule.principal_minor), depositMinor: String(schedule.deposit_minor), startsOn: schedule.starts_on, dueOn: schedule.due_on } : null, latestClosure: latest ? { id: latest.id, version: latest.version, periodStart: latest.period_start, periodEnd: latest.period_end, revenueMinor: String(latest.gross_revenue_ex_tax_minor), costsMinor: String(latest.costs_and_refunds_minor), profitMinor: String(latest.distributable_profit_minor) } : null, closures: bookClosures.map((value) => ({ id: value.id, version: value.version, periodStart: value.period_start, periodEnd: value.period_end, supersedesClosureId: value.supersedes_closure_id, ruleVersionId: value.rule_version_id, cutoffAt: value.entry_cutoff_at, revenueMinor: String(value.gross_revenue_ex_tax_minor), costsMinor: String(value.costs_and_refunds_minor), profitMinor: String(value.distributable_profit_minor), contentHash: value.content_hash, closedAt: value.closed_at, allocations: allocations.filter((allocationValue) => allocationValue.closure_version_id === value.id).map((allocationValue) => ({ id: allocationValue.id, beneficiaryCode: allocationValue.beneficiary_code, shareBps: allocationValue.share_bps, amountMinor: String(allocationValue.amount_minor), roundingAdjustmentMinor: allocationValue.rounding_adjustment_minor })) })), allocations: closureAllocations.map((v) => ({ beneficiaryCode: v.beneficiary_code, shareBps: v.share_bps, amountMinor: String(v.amount_minor) })), balances: beneficiaryCodes.map((code) => ({ beneficiaryCode: code, amountMinor: sumExact(bookEntries.filter((v) => v.beneficiary_code === code).map((v) => String(v.amount_minor))) })), ledgerEntries: bookEntries.map((value) => ({ id: String(value.id), closureVersionId: value.closure_version_id, beneficiaryCode: value.beneficiary_code, entryType: value.entry_type, amountMinor: String(value.amount_minor), referenceType: value.reference_type, referenceId: value.reference_id, proofHash: value.proof_hash, createdAt: value.created_at })), entryFeeLedger: feeMovements.map((value) => ({ id: String(value.id), entryType: value.entry_type, amountMinor: String(value.amount_minor), createdAt: value.created_at })) };
    }),
  } };
}
