import{describe,expect,it}from"vitest";import{scoringPolicy}from"./model";describe("scoring policy",()=>{it("is versioned, bilingual and deterministically hashed",()=>{const id="00000000-0000-4000-8000-000000000001",a=scoringPolicy(id),b=scoringPolicy(id);expect(a.content_hash).toBe(b.content_hash);expect(a.content_hash).toMatch(/^[0-9a-f]{64}$/u);expect(a.recommendations.CRITICAL.title_ar).toContain("تم")})});
