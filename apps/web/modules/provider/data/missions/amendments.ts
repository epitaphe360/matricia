import{z}from"zod";import{getSupabaseServerClient}from"@/modules/shared/lib/supabase/server";const uuid=z.string().uuid(),hash=z.string().regex(/^[0-9a-f]{64}$/),amount=z.union([z.string().regex(/^-?\d+$/),z.number().int().safe()]).transform(String);const row=z.object({id:uuid,contract_id:uuid,amendment_number:z.number().int().positive(),status:z.enum(["DRAFT","PENDING_SIGNATURE","ACTIVE","REJECTED","SUPERSEDED"]),price_delta_minor:amount,deadline_delta_days:z.number().int(),content_hash:hash,activated_contract_version_id:uuid.nullable(),created_at:z.string()});const sig=z.object({amendment_id:uuid,organization_id:uuid,signed_at:z.string()});export type ProviderAmendmentDashboard={organizationId:string;items:Array<{id:string;contractId:string;number:number;status:z.infer<typeof row>["status"];priceDeltaMinor:string;deadlineDeltaDays:number;contentHash:string;activatedContractVersionId:string|null;createdAt:string;signatures:Array<{organizationId:string;signedAt:string}>}>};
export async function loadProviderAmendments():Promise<{status:"success";value:ProviderAmendmentDashboard}|{status:"error"}>{const c=await getSupabaseServerClient(),{data:a}=await c.auth.getUser();if(!a.user)return{status:"error"};const m=await c.from("organization_memberships").select("organization_id,organization_member_roles!inner(role_code,revoked_at)").eq("user_id",a.user.id).eq("status","ACTIVE").is("organization_member_roles.revoked_at",null).in("organization_member_roles.role_code",["PROVIDER_OWNER","PROVIDER_MANAGER"]).limit(1).maybeSingle(),org=z.object({organization_id:uuid}).safeParse(m.data);if(m.error||!org.success)return{status:"error"};const contracts=await c.from("contracts").select("id").eq("provider_organization_id",org.data.organization_id).limit(100),ids=z.array(z.object({id:uuid})).safeParse(contracts.data);if(contracts.error||!ids.success)return{status:"error"};if(!ids.data.length)return{status:"success",value:{organizationId:org.data.organization_id,items:[]}};const amendments=await c.from("contract_amendments").select("id,contract_id,amendment_number,status,price_delta_minor,deadline_delta_days,content_hash,activated_contract_version_id,created_at").in("contract_id",ids.data.map(x=>x.id)).order("created_at",{ascending:false}).limit(200),parsed=z.array(row).safeParse(amendments.data);if(amendments.error||!parsed.success)return{status:"error"};const signatures=parsed.data.length?await c.from("contract_amendment_signatures").select("amendment_id,organization_id,signed_at").in("amendment_id",parsed.data.map(x=>x.id)).limit(400):{data:[],error:null},signed=z.array(sig).safeParse(signatures.data);if(signatures.error||!signed.success)return{status:"error"};return{status:"success",value:{organizationId:org.data.organization_id,items:parsed.data.map(x=>({id:x.id,contractId:x.contract_id,number:x.amendment_number,status:x.status,priceDeltaMinor:x.price_delta_minor,deadlineDeltaDays:x.deadline_delta_days,contentHash:x.content_hash,activatedContractVersionId:x.activated_contract_version_id,createdAt:x.created_at,signatures:signed.data.filter(s=>s.amendment_id===x.id).map(s=>({organizationId:s.organization_id,signedAt:s.signed_at}))}))}}}
