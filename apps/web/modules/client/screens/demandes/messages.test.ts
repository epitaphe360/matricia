import { describe,expect,it } from "vitest";import { getClientRfqMessages } from "./messages";describe("messages RFQ Client",()=>{it("garde les mêmes clés FR et AR",()=>expect(Object.keys(getClientRfqMessages("ar")).sort()).toEqual(Object.keys(getClientRfqMessages("fr")).sort()));it("localise les états et le texte critique",()=>{expect(getClientRfqMessages("ar").statuses.PROVIDER_SELECTED).not.toBe(getClientRfqMessages("fr").statuses.PROVIDER_SELECTED);expect(getClientRfqMessages("ar").privacy).toContain("مقدم")})});
