import{beforeEach,describe,expect,it,vi}from"vitest";const command=vi.fn();vi.mock("@/modules/shared/lib/disputes/server-repository",()=>({createServerDisputesRepository:async()=>({command})}));vi.mock("next/cache",()=>({revalidatePath:vi.fn()}));import{openAction,proposeAction}from"./actions";
function form(values:Record<string,string>){const f=new FormData();for(const[k,v]of Object.entries(values))f.set(k,v);return f}const base={locale:"fr",idempotencyKey:"00000000-0000-4000-8000-000000000001"};beforeEach(()=>command.mockReset());
describe("dispute server actions",()=>{it("does not invoke RPC for invalid evidence hash",async()=>{const r=await openAction({status:"idle"},form({...base,missionId:"00000000-0000-4000-8000-000000000002",obligationKey:"DELIVERY.DUE",description:"Retard constaté avec preuve datée.",urgency:"STANDARD",policyVersion:"v1",policyHash:"a".repeat(64),responseHours:"48",urgentHours:"12",correctionDays:"5",reviewDays:"5",appealHours:"48",evidenceStatement:"preuve",evidenceHash:"bad",visibility:"BOTH_PARTIES"}));expect(r).toEqual({status:"error",reason:"VALIDATION"});expect(command).not.toHaveBeenCalled()});it("rejects a new-quote reassignment without quote version",async()=>{const r=await proposeAction({status:"idle"},form({...base,caseId:"00000000-0000-4000-8000-000000000002",reassignmentId:"00000000-0000-4000-8000-000000000003",providerId:"00000000-0000-4000-8000-000000000004",pricing:"NEW_QUOTE",quoteVersion:"",cost:"100,00",rowVersion:"1"}));expect(r).toEqual({status:"error",reason:"VALIDATION"});expect(command).not.toHaveBeenCalled()})});
