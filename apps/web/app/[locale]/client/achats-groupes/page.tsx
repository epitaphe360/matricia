import Link from"next/link";import{notFound,redirect}from"next/navigation";import{Alert,AlertDescription,AlertTitle}from"@/components/ui/alert";import{buttonVariants}from"@/components/ui/button";import{isLocale}from"@/lib/i18n/locale";import{loadVolumeProcurement}from"@/lib/volume-procurement/repository";import{cn}from"@/lib/utils";import{getVolumeMessages}from"./messages";import{VolumePanel}from"./volume-panel";export default async function VolumePage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const m=getVolumeMessages(locale),r=await loadVolumeProcurement();if(r.status==="error"&&r.reason==="UNAUTHENTICATED")redirect(`/${locale}/connexion`);const alt=locale==="fr"?"ar":"fr";return <main className="min-h-dvh bg-muted/40 px-4 py-6 sm:px-6 sm:py-8"><div className="mx-auto max-w-7xl space-y-7"><nav aria-label="Navigation"className="flex flex-wrap items-center justify-between gap-3"><Link href={`/${locale}/tableau-de-bord`}className={cn(buttonVariants({variant:"outline"}),"min-h-11")}>{m.back}</Link><Link href={`/${alt}/client/achats-groupes`}hrefLang={alt}className="min-h-11 rounded-md px-3 py-2 font-medium text-primary focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2">{m.language}</Link></nav><header><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">{r.status==="success"?r.dashboard.organizationName:"Matricia"}</p><h1 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">{m.title}</h1><p className="mt-3 max-w-3xl leading-7 text-muted-foreground">{m.description}</p></header>{r.status==="error"?<Alert variant={r.reason==="NO_CLIENT_ORGANIZATION"?"default":"destructive"}><AlertTitle>{r.reason==="NO_CLIENT_ORGANIZATION"?m.noOrg:m.loadError}</AlertTitle><AlertDescription>{r.reason}</AlertDescription></Alert>:<VolumePanel dashboard={r.dashboard}locale={locale}m={m}keys={[crypto.randomUUID(),crypto.randomUUID(),crypto.randomUUID()]}/>}</div></main>}
