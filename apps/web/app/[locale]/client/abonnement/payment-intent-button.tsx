"use client";
import{useRef,useState}from"react";import{Button}from"@/components/ui/button";import type{SubscriptionMessages}from"./messages";
type Props={locale:"fr"|"ar";organizationId:string;planVersionId:string;billingInterval:"MONTHLY"|"ANNUAL";messages:SubscriptionMessages};
export function PaymentIntentButton({locale,organizationId,planVersionId,billingInterval,messages}:Props){const key=useRef<string>(crypto.randomUUID()),[state,setState]=useState<"idle"|"pending"|"success"|"error">("idle");async function create(){setState("pending");try{const response=await fetch("/api/payments/intents",{method:"POST",credentials:"same-origin",headers:{"content-type":"application/json"},body:JSON.stringify({locale,organizationId,planVersionId,billingInterval,idempotencyKey:key.current})});if(!response.ok){setState("error");return}const value:unknown=await response.json();if(!isIntent(value)){setState("error");return}setState("success");}catch{setState("error")}}return <div className="space-y-2"><Button type="button" variant={billingInterval==="ANNUAL"?"default":"outline"} className="min-h-11 w-full" disabled={state==="pending"||state==="success"} onClick={create}>{state==="pending"?messages.pending:billingInterval==="MONTHLY"?messages.paymentMonthly:messages.paymentAnnual}</Button>{state==="success"?<p role="status" className="text-sm text-primary">{messages.paymentCreated}</p>:state==="error"?<p role="alert" className="text-sm text-destructive">{messages.paymentError}</p>:null}</div>}
function isIntent(value:unknown):value is{paymentIntentId:string;status:"REQUIRES_CONFIRMATION"}{return typeof value==="object"&&value!==null&&"paymentIntentId"in value&&typeof value.paymentIntentId==="string"&&"status"in value&&value.status==="REQUIRES_CONFIRMATION"}
