import{randomUUID}from"node:crypto";import Link from"next/link";import{notFound,redirect}from"next/navigation";import{buttonVariants}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{createServerDisputesRepository}from"@/lib/disputes/server-repository";import{isLocale}from"@/lib/i18n/locale";import{cn}from"@/lib/utils";import{getDisputeMessages}from"../messages";import{OpenForm}from"../open-form";
export default async function NewDispute({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const result=await(await createServerDisputesRepository()).list();if(result.status==="error"&&result.reason==="UNAUTHENTICATED")redirect(`/${locale}/connexion`);if(result.status==="error")throw new Error("DISPUTES_UNAVAILABLE");const m=getDisputeMessages(locale);return <main className="min-h-dvh bg-muted/40 px-4 py-6 sm:px-6"><div className="mx-auto max-w-4xl space-y-5"><Link href={`/${locale}/client/litiges`} className={cn(buttonVariants({variant:"outline"}),"min-h-11")}>{m.back}</Link><Card><CardHeader><CardTitle>{m.newCase}</CardTitle></CardHeader><CardContent><OpenForm locale={locale} missions={result.value.missions} m={m} keyValue={randomUUID()}/></CardContent></Card></div></main>}
