import{randomUUID}from"node:crypto";import Link from"next/link";import{notFound,redirect}from"next/navigation";import{Badge}from"@/components/ui/badge";import{buttonVariants}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{createServerDisputesRepository}from"@/lib/disputes/server-repository";import{isLocale}from"@/lib/i18n/locale";import{cn}from"@/lib/utils";import{ActionPanel}from"../action-panel";import{getDisputeMessages}from"../messages";
export default async function DisputePage({params}:{params:Promise<{locale:string;caseId:string}>}){const{locale,caseId}=await params;if(!isLocale(locale))notFound();const result=await(await createServerDisputesRepository()).detail(caseId);if(result.status==="error"&&result.reason==="UNAUTHENTICATED")redirect(`/${locale}/connexion`);if(result.status==="error"||!result.value)notFound();const d=result.value,m=getDisputeMessages(locale),keys=Object.fromEntries(["respond","appeal","decide","propose","approve","activate"].map(x=>[x,randomUUID()]));return <main className="min-h-dvh bg-muted/40 px-4 py-6 sm:px-6"><div className="mx-auto max-w-6xl space-y-6"><Link href={`/${locale}/client/litiges`} className={cn(buttonVariants({variant:"outline"}),"min-h-11")}>{m.back}</Link><Card><CardHeader><div className="flex flex-wrap justify-between gap-3"><CardTitle>{d.obligationKey}</CardTitle><Badge>{m.statuses[d.status]}</Badge></div></CardHeader><CardContent className="space-y-3"><p className="whitespace-pre-wrap leading-7">{d.description}</p><dl className="grid gap-2 text-sm sm:grid-cols-3"><div><dt className="text-muted-foreground">{m.policy}</dt><dd>{d.policyVersion}</dd></div><div><dt className="text-muted-foreground">{m.deadline}</dt><dd>{d.responseDueAt}</dd></div><div><dt className="text-muted-foreground">{m.mission}</dt><dd dir="ltr">{d.missionId}</dd></div></dl></CardContent></Card><section aria-labelledby="evidence-title"><h2 id="evidence-title" className="mb-3 text-xl font-semibold">{m.evidence}</h2><div className="grid gap-3 md:grid-cols-2">{d.evidence.map(x=><article key={x.id} className="rounded-xl border bg-card p-4"><div className="flex justify-between gap-2"><strong>{x.type}</strong><Badge variant="outline">{x.visibility}</Badge></div><p className="mt-2 whitespace-pre-wrap">{x.statement??x.url}</p><code className="mt-2 block break-all text-xs" dir="ltr">{x.hash}</code></article>)}</div></section><section aria-labelledby="timeline-title"><h2 id="timeline-title" className="mb-3 text-xl font-semibold">{m.timeline}</h2><ol className="space-y-2 border-s ps-5">{d.events.map(x=><li key={x.id}><strong>{x.type}</strong><span className="block text-sm text-muted-foreground">{x.from??"—"} → {x.to} · {x.createdAt}</span></li>)}</ol></section>{d.reassignment?<Card><CardHeader><CardTitle>{m.reassignment} {d.reassignment.key}</CardTitle></CardHeader><CardContent><p>{d.reassignment.status} · <span dir="ltr">{d.reassignment.costDeltaMinor??"0"} {d.reassignment.currency}</span></p></CardContent></Card>:null}<section aria-labelledby="actions-title"><h2 id="actions-title" className="mb-3 text-xl font-semibold">{m.actions}</h2><ActionPanel locale={locale} d={d} m={m} keys={keys}/></section></div></main>}
