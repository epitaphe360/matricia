import{describe,expect,it,vi}from"vitest";const complete=vi.fn(),transition=vi.fn();vi.mock("../../../../lib/diagnostics-opportunities/server-repository",()=>({createServerDiagnosticsRepository:async()=>({complete,transition})}));vi.mock("next/cache",()=>({revalidatePath:vi.fn()}));import{completeAction,opportunityAction}from"./actions";function form(v:Record<string,string>){const f=new FormData();Object.entries(v).forEach(([k,x])=>f.set(k,x));return f}const base={locale:"fr",idempotencyKey:"00000000-0000-4000-8000-000000000001"};describe("diagnostic actions",()=>{it("rejects malformed completion before RPC",async()=>{expect(await completeAction({status:"idle"},form({...base,sessionId:"bad",serviceId:"bad"}))).toEqual({status:"error",reason:"VALIDATION"});expect(complete).not.toHaveBeenCalled()});it("requires a date to defer",async()=>{expect(await opportunityAction({status:"idle"},form({...base,runId:"00000000-0000-4000-8000-000000000002",opportunityId:"00000000-0000-4000-8000-000000000003",action:"DEFER",deferredUntil:"",rowVersion:"1"}))).toEqual({status:"error",reason:"VALIDATION"});expect(transition).not.toHaveBeenCalled()})});
