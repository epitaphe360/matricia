"use server";import{randomUUID}from"node:crypto";import{revalidatePath}from"next/cache";import{z}from"zod";import{createServerSolutionInsightsRepository}from"@/lib/solution-insights/server-repository";import{solutionDecision,solutionLevel,uuid}from"@/lib/solution-insights/model";
export type State={status:"idle"|"success"|"error";reason?:"VALIDATION"|"COMMAND"};export const idle:State={status:"idle"};
export async function decideAction(_:State,form:FormData):Promise<State>{const parsed=z.object({locale:z.enum(["fr","ar"]),solutionSetId:uuid,level:solutionLevel,decision:solutionDecision,reason:z.string().trim().min(3).max(2000),deferredUntil:z.union([z.string().regex(/^\d{4}-\d{2}-\d{2}$/u),z.literal("")]),idempotencyKey:uuid}).safeParse(Object.fromEntries(form.entries()));if(!parsed.success||(parsed.data.decision==="DEFERRED")!==Boolean(parsed.data.deferredUntil))return{status:"error",reason:"VALIDATION"};const p=parsed.data;const result=await(await createServerSolutionInsightsRepository()).decide({solutionSetId:p.solutionSetId,level:p.level,decision:p.decision,reason:p.reason,deferredUntil:p.deferredUntil?`${p.deferredUntil}T23:59:59Z`:null,idempotencyKey:p.idempotencyKey,correlationId:randomUUID()});if(result.status==="error")return{status:"error",reason:"COMMAND"};revalidatePath(`/${p.locale}/client/diagnostics/solutions`);return{status:"success"}}
