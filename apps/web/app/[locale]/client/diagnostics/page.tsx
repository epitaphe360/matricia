import{randomUUID}from"node:crypto";import Link from"next/link";import{notFound,redirect}from"next/navigation";import{Badge}from"@/components/ui/badge";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{formatExactScore}from"@/lib/diagnostics-opportunities/model";import{createServerDiagnosticsRepository}from"@/lib/diagnostics-opportunities/server-repository";import{isLocale}from"@/lib/i18n/locale";import{CompleteForm}from"./complete-form";import{messages}from"./messages";export default async function Diagnostics({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const r=await(await createServerDiagnosticsRepository()).list();if(r.status==="error"&&r.reason==="UNAUTHENTICATED")redirect(`/${locale}/connexion`);if(r.status==="error")throw new Error("DIAGNOSTICS_UNAVAILABLE");const m=messages(locale);return <main className="min-h-dvh bg-muted/40 px-4 py-6 sm:px-6" dir={locale==="ar"?"rtl":"ltr"}><div className="mx-auto max-w-6xl space-y-6"><header><h1 className="text-3xl font-semibold">{m.title}</h1><p className="mt-2 max-w-3xl text-muted-foreground">{m.intro}</p></header><CompleteForm locale={locale} sessions={r.value.sessions} services={r.value.services} m={m} keyValue={randomUUID()}/><section aria-labelledby="runs"><h2 id="runs" className="mb-3 text-xl font-semibold">{m.runs}</h2>{!r.value.runs.length?<p className="rounded-xl border bg-card p-5">{m.empty}</p>:<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{r.value.runs.map(x=><Card key={x.id}><CardHeader><div className="flex justify-between gap-2"><CardTitle><span dir="ltr">{formatExactScore(x.score)}/100</span></CardTitle><Badge>{m.ratings[x.rating]}</Badge></div></CardHeader><CardContent><p>{new Date(x.completedAt).toLocaleString(locale)}</p><Link href={`/${locale}/client/diagnostics/${x.id}`} className="mt-3 inline-flex min-h-11 items-center font-medium underline">{m.view}</Link></CardContent></Card>)}</div>}</section></div></main>}
