import{randomUUID}from"node:crypto";import Link from"next/link";import{notFound,redirect}from"next/navigation";import{Alert,AlertDescription}from"@/components/ui/alert";import{buttonVariants}from"@/components/ui/button";import{loadClientDocumentVault}from"@/lib/client-documents/server-repository";import{isLocale}from"@/lib/i18n/locale";import{cn}from"@/lib/utils";import{DocumentVaultPanel}from"./document-vault-panel";import{messages}from"./messages";
export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const result=await loadClientDocumentVault();if(result.status==="error"&&result.reason==="UNAUTHENTICATED")redirect(`/${locale}/connexion`);const m=messages[locale],alt=locale==="fr"?"ar":"fr";return <main className="min-h-dvh bg-muted/40 px-4 py-6 sm:px-6"><div className="mx-auto max-w-6xl space-y-7"><nav aria-label={m.nav} className="flex flex-wrap justify-between gap-3"><Link href={`/${locale}/tableau-de-bord`} className={cn(buttonVariants({variant:"outline"}),"min-h-11")}>{m.back}</Link><Link href={`/${alt}/client/documents`} hrefLang={alt} className="min-h-11 px-3 py-2 text-primary underline">{m.language}</Link></nav><header><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">{m.eyebrow}</p><h1 className="mt-2 text-3xl font-semibold">{m.title}</h1><p className="mt-3 max-w-3xl text-muted-foreground">{m.description}</p></header><Alert><AlertDescription>{m.safety}</AlertDescription></Alert>{result.status==="error"?<p role="alert" className="text-destructive">{m.loadError}</p>:<DocumentVaultPanel locale={locale} data={result.value} keys={{link:randomUUID(),revoke:Object.fromEntries(result.value.bindings.map(x=>[x.id,randomUUID()]))}}/>}</div></main>}
