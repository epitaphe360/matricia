import{describe,expect,it}from"vitest";import{getBillingMessages}from"./messages";describe("provider billing copy",()=>{it("keeps FR and AR key parity",()=>expect(Object.keys(getBillingMessages("ar"))).toEqual(Object.keys(getBillingMessages("fr"))));it("contains native Arabic and user-facing financial guidance",()=>{expect(getBillingMessages("ar").title).toMatch(/[\u0600-\u06ff]/u);expect(getBillingMessages("fr").amountInput).toContain("MAD");expect(getBillingMessages("fr").proof).not.toContain("SHA")})});
