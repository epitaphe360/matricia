import Link from"next/link";import{notFound,redirect}from"next/navigation";import{z}from"zod";import{isLocale}from"@/lib/i18n/locale";import{getSupabaseServerClient}from"@/lib/supabase/server";import{FinanceApprovalPanel}from"./panel";
const id=z.string().uuid(),request=z.object({id,status:z.string(),row_version:z.number().int().positive(),resource_type:z.string(),resource_id:z.string()}),forecast=z.object({id,mission_id:id,estimated_commission_minor:z.union([z.number(),z.string()]).transform(String),currency:z.string()}),campaign=z.object({id,title_fr:z.string(),title_ar:z.string(),row_version:z.number().int().positive()}),mission=z.object({id}),payable=z.object({id,mission_id:id,commission_amount_minor:z.union([z.number(),z.string()]).transform(String),currency:z.string()});
export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const c=await getSupabaseServerClient(),{data:auth}=await c.auth.getUser();if(!auth.user)redirect(`/${locale}/connexion`);const[q,f,mc,mi,pe]=await Promise.all([c.from("business_approval_requests").select("id,status,row_version,resource_type,resource_id").order("requested_at",{ascending:false}).limit(300),c.from("provider_commission_forecasts").select("id,mission_id,estimated_commission_minor,currency").order("created_at",{ascending:false}).limit(300),c.from("marketing_campaigns").select("id,title_fr,title_ar,row_version").eq("status","VALIDATED_BY_RULES").order("created_at",{ascending:false}).limit(200),c.from("missions").select("id").in("status",["ACTIVE","DELIVERY_SUBMITTED","ACCEPTANCE_IN_PROGRESS"]).limit(200),c.from("provider_payable_events").select("id,mission_id,commission_amount_minor,currency").eq("event_type","COMMISSION_ACCRUAL").limit(300)]),requests=z.array(request).safeParse(q.data),forecasts=z.array(forecast).safeParse(f.data),campaigns=z.array(campaign).safeParse(mc.data),missions=z.array(mission).safeParse(mi.data),payables=z.array(payable).safeParse(pe.data),ar=locale==="ar",failed=[q,f,mc,mi,pe].some(x=>x.error)||!requests.success||!forecasts.success||!campaigns.success||!missions.success||!payables.success;return <main className="min-h-dvh bg-muted/40 px-4 py-6" dir={ar?"rtl":"ltr"}><div className="mx-auto max-w-6xl space-y-6"><Link href={`/${locale}/administration/command-center`} className="underline">{ar?"مركز القيادة":"Centre de commande"}</Link><header><h1 className="text-3xl font-semibold">{ar?"موافقات الأعمال والعمولات":"Approbations métier et commissions"}</h1><p className="mt-2 text-muted-foreground">{ar?"ضوابط AAL2 وفصل المهام ومبالغ صحيحة ومطابقة واحد لواحد.":"Contrôles AAL2, séparation des tâches, montants exacts et réconciliation strictement un-à-un."}</p></header>{failed?<p role="alert">{ar?"تعذر تحميل البيانات المصرح بها.":"Impossible de charger les données autorisées."}</p>:<FinanceApprovalPanel locale={locale} requests={requests.data} forecasts={forecasts.data} campaigns={campaigns.data} missions={missions.data} payables={payables.data}/>}</div></main>}
