import{randomUUID}from"node:crypto";import Link from"next/link";import{notFound}from"next/navigation";import{buttonVariants}from"@/components/ui/button";import{isLocale}from"@/lib/i18n/locale";import{cn}from"@/lib/utils";import{ClonePanel}from"./clone-panel";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const ar=locale==="ar";return <main className="min-h-dvh bg-muted/40 px-4 py-6 sm:px-6"><div className="mx-auto max-w-7xl space-y-7"><nav><Link href={"/"+locale+"/tableau-de-bord"} className={cn(buttonVariants({variant:"outline"}),"min-h-11")}>{ar?"لوحة التحكم":"Tableau de bord"}</Link></nav><header><h1 className="text-3xl font-semibold">{ar?"استنساخ المحتوى مع المصدر":"Clonage de contenu avec provenance"}</h1><p className="mt-3 text-muted-foreground">{ar?"يتم إنشاء نسخ جديدة دون تعديل المصدر، مع بصمات وتدقيق وصندوق أحداث.":"Chaque clone crée une nouvelle version sans modifier la source, avec empreintes, audit et Event Outbox."}</p></header><ClonePanel locale={locale} keys={[randomUUID(),randomUUID(),randomUUID()]}/></div></main>}
