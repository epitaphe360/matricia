import{randomUUID}from"node:crypto";import Link from"next/link";import{notFound,redirect}from"next/navigation";import{Alert,AlertDescription,AlertTitle}from"@/components/ui/alert";import{buttonVariants}from"@/components/ui/button";import{isLocale}from"@/lib/i18n/locale";import{loadMarketingDashboard}from"@/lib/marketing-autopilot/repository";import{cn}from"@/lib/utils";import{MarketingPanel}from"./marketing-panel";import{getMarketingMessages}from"./messages";
export default async function MarketingAutopilotPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const m=getMarketingMessages(locale),result=await loadMarketingDashboard();if(result.status==="error"&&result.reason==="UNAUTHENTICATED")redirect(`/${locale}/connexion`);const keys:Record<string,string>={consent:randomUUID(),campaign:randomUUID()};if(result.status==="success")result.dashboard.campaigns.forEach(v=>{keys[`approve:${v.id}`]=randomUUID();keys[`schedule:${v.id}`]=randomUUID()});return <main className="min-h-dvh bg-muted/40 px-4 py-6 sm:px-6 sm:py-8" dir={locale==="ar"?"rtl":"ltr"}><div className="mx-auto max-w-7xl space-y-7"><nav><Link href={`/${locale}/administration/command-center`} className={cn(buttonVariants({variant:"outline"}),"min-h-11")}>← {locale==="ar"?"مركز القيادة":"Centre de commande"}</Link></nav><header><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">{m.eyebrow}</p><h1 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">{m.title}</h1><p className="mt-3 max-w-3xl leading-7 text-muted-foreground">{m.subtitle}</p></header>{result.status==="error"?<Alert variant="destructive"><AlertTitle>{result.reason==="FORBIDDEN"?m.accessDenied:m.loadFailed}</AlertTitle><AlertDescription>{result.reason}</AlertDescription></Alert>:<MarketingPanel dashboard={result.dashboard} locale={locale} m={m} keys={keys}/>}</div></main>}
