import{randomUUID}from"node:crypto";import{notFound,redirect}from"next/navigation";import{Alert,AlertDescription,AlertTitle}from"@/components/ui/alert";import{isLocale}from"@/lib/i18n/locale";import{loadFranchiseFollowups}from"@/lib/franchise-followups/repository";import{FollowupPanel}from"./panel";import{getFollowupMessages}from"./messages";
export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const m=getFollowupMessages(locale),r=await loadFranchiseFollowups();if(r.status==="error"&&r.reason==="UNAUTHENTICATED")redirect(`/${locale}/connexion`);return <main className="min-h-dvh bg-muted/40 px-4 py-6 sm:px-6" dir={locale==="ar"?"rtl":"ltr"}><div className="mx-auto max-w-7xl space-y-7"><header><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">{m.eyebrow}</p><h1 className="mt-2 text-3xl font-semibold">{m.title}</h1><p className="mt-3 max-w-3xl text-muted-foreground">{m.subtitle}</p></header>{r.status==="error"?<Alert variant="destructive"><AlertTitle>{r.reason==="FORBIDDEN"?m.accessDenied:m.loadFailed}</AlertTitle><AlertDescription>{r.reason}</AlertDescription></Alert>:<FollowupPanel dashboard={r.dashboard} locale={locale} m={m} keyValue={randomUUID()}/>}</div></main>}
