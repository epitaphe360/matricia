import {beforeEach,describe,expect,it,vi}from"vitest";
const mocks=vi.hoisted(()=>({rpc:vi.fn()}));
vi.mock("@/lib/supabase/admin",()=>({getSupabaseAdminClient:()=>({rpc:mocks.rpc})}));
import{GET}from"./route";
const token="a".repeat(128),request=(cookie?:string)=>new Request(`https://matricia.ma/api/marketing/cta/${token}`,{headers:{"user-agent":"browser","x-forwarded-for":"192.0.2.1","x-request-id":"req-1",...(cookie?{cookie}:{})}});
describe("public marketing CTA",()=>{beforeEach(()=>{process.env.MARKETING_CTA_SIGNING_SECRET="s".repeat(32);delete process.env.VERCEL;mocks.rpc.mockReset()});it("resolves a signed opaque token and issues a signed visitor cookie",async()=>{mocks.rpc.mockResolvedValue({error:null,data:{outcome:"MARKETING_CTA_RESOLVED",organization_id:"11111111-1111-4111-8111-111111111111",campaign_id:"22222222-2222-4222-8222-222222222222",content_id:"33333333-3333-4333-8333-333333333333",service_id:"44444444-4444-4444-8444-444444444444",destination_url:"https://matricia.ma/diagnostic?campaign_id=2"}});const response=await GET(request(),{params:Promise.resolve({token})});expect(response.status).toBe(302);expect(response.headers.get("location")).toContain("/diagnostic");expect(response.headers.get("set-cookie")).toContain("HttpOnly");expect(mocks.rpc).toHaveBeenCalledWith("resolve_marketing_cta_v1",expect.objectContaining({p_token:token,p_visitor_hash:expect.stringMatching(/^[0-9a-f]{64}$/),p_idempotency_key:expect.stringMatching(/^[0-9a-f]{64}$/)}))});it("fails closed without a signing secret",async()=>{delete process.env.MARKETING_CTA_SIGNING_SECRET;expect((await GET(request(),{params:Promise.resolve({token})})).status).toBe(503);expect(mocks.rpc).not.toHaveBeenCalled()});});
